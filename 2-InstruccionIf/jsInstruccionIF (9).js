function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	var numeroRamdom;
	 numeroRandom = Math.floor ( Math.random () * 10 + 1);  // esta es la formula de la secuencia o comando para sacar el numero random// 

	 alert (numeroRandom);

	// el comando de random no lo vamos a usar pero es un conocimiento mas //
	// random: sirve para sacar un un numero random (aleatorio) en un rango determinado.

}//FIN DE LA FUNCIÓN