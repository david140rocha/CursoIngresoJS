function mostrar()
{
   // var contador;   ///a la variable se le da valor en for pero si queres la declaras ó no , es lo mismo. 
    for(contador = 0 ; contador <=10 ; contador++){
        document.write(contador)
    }

}