function mostrar()
{
	var contador = 0
	while (contador <=9){   // el contador solo va contar hasta donde le pongamos el limite ej... while (contador <= 9)//significa que va contar del 0 hasta el 9 pero depende cuanto vale el contador o la variable el contador puede contar desde el numero que le demos de valor ej

		contador = contador + 1 // se puede resumir en ... contador ++ //
		console.log (contador);//se puede poner alert pero vas a tener que andar cerrando cada rato el letrero y con console,log no es necesario//
	}

}//FIN DE LA FUNCIÓN