function mostrar()
{
	var numeros = 3
	var contador = 3
	while (contador <=9){   // el contador solo va contar hasta donde le pongamos el limite ej... while (contador <= 9)//significa que va contar del 0 hasta el 9 pero depende cuanto vale el contador o la variable el contador puede contar desde el numero que le demos de valor a la variable del contador.
		numeros = numeros + 1 // se puede  resumir como ....ej. numeros ++ //funciona igual
		contador = contador + 1 // igual se puede resumir en ... contador ++ //
		alert(contador);
	}

}//FIN DE LA FUNCIÓN